# PROGRESS.md — журнал работы

Файл ведётся построчно: каждый выполненный шаг отмечается `[x]` с датой и короткой
записью «что сделано / как проверено». Так работу можно бросить в любой момент и
продолжить с первого незакрытого пункта.

Статусы: `[ ]` не начат · `[~]` в работе · `[x]` готов и проверен · `[!]` заблокирован

---

## Окружение (зафиксировано 2026-08-18)

- Docker в системе **не установлен**. Разработка идёт на локальном PostgreSQL 18.3
  (Homebrew, уже запущен). `docker-compose.yml` пишется для паритета с продом,
  но локально не используется.
- Клиент `psql` в PATH — версии 16, сервер 18. Для CLI использовать
  `/opt/homebrew/opt/postgresql@18/bin/psql`. Prisma работает по сети, разницы не видит.
- Node v25.8.1, npm 11.11.0.

## Открытые вопросы, влияющие на этап 1

- **[решено по ходу] Один телефон на семью или на человека?** Реализован
  вариант «один `Guest` = один человек». Поиск возвращает до 5 совпадений
  списком, поэтому телефон, ищущий «Петров», увидит и мужа, и жену и выберет
  нужного — семейный сценарий работает без отдельной сущности группы.
  Структура таблиц под группу готова (`parentGuestId`), API менять не придётся.

---

## Этап 0. Каркас

- [x] **0.1** Next.js 15 + TS + Tailwind + shadcn, структура папок из PLAN.md §6
- [x] **0.2** БД и окружение: `.env`, `docker-compose.yml`, локальная база
- [x] **0.3** Prisma schema (Org/User/Membership/Event/Guest/SeatTable/Seat), миграция, `pg_trgm`
- [x] **0.4** Аутентификация организатора: сессия на cookie, argon2
- [x] **0.5** Seed: 2 организации × 2 мероприятия с пересекающимися именами гостей
- [x] **0.6** Репозитории + Prisma-страж по `eventId` + тест `tenancy.spec.ts`

## Этап 3. Конструктор рассадки

- [x] **3.1** Общая геометрия плана: одна формула на редактор, просмотр и PDF
- [x] **3.2** Атомарные операции с версией мероприятия + API
- [x] **3.3** Перетаскивание гостя на место, столов по залу (@dnd-kit)
- [x] **3.4** Список нерассаженных, автосохранение, оптимистичный отклик
- [x] **3.5** Отмена последнего действия
- [x] **3.6** Конфликт двух вкладок: 409 и понятное сообщение
- [x] **3.7** Тесты операций рассадки
- [~] **3.8** Проверка: план на 40 гостей и запасной путь — да; **тач на живом планшете не проверен**

## Этап 2. PDF и план Б

- [x] **2.1** Шрифт с кириллицей и каркас генерации PDF
- [x] **2.2** План зала в PDF: столы, места, имена
- [x] **2.3** Списки: по столам и по алфавиту
- [x] **2.4** Маршрут выгрузки + кнопка в панели
- [x] **2.5** Проверка печатью: A4 и A3, кириллица, длинные имена

## Этап 1. Вход в зал

- [x] **1.1** Нормализация имён, словарь уменьшительных, `guest-search`, тест на опечатках
- [x] **1.2** Импорт гостей CSV + список гостей в панели организатора
- [x] **1.3** Примитивная рассадка: столы и назначение гостя на место (без drag-n-drop)
- [x] **1.4** `/e/[shortCode]` — статическая страница входа + `POST /lookup` + рейт-лимит
- [x] **1.5** `/e/[shortCode]/me` — «ваш стол», `claim` и cookie `guest_session`
- [x] **1.6** Service Worker: оболочка `/e/*` и кеш последнего ответа «мой стол»
- [x] **1.7** Печатные материалы: QR с `shortCode` + список «имя → стол» на бумагу
- [x] **1.8** Замеры по критериям PLAN.md: вес страниц, p95 lookup, путь на Slow 3G

---

## Журнал

### 2026-08-18 — 0.1 Каркас приложения
Сделано: `create-next-app` (TS, Tailwind 4, App Router, src/, eslint, alias `@/*`),
проект перенесён в каталог `вместе` (кириллическое имя каталога npm не принимает
как имя пакета — поэтому скаффолд собран во временной папке и перенесён).
**Отклонение от PLAN.md:** установился **Next.js 16.3.1**, а не 15 — это текущий
стабильный релиз, App Router и все нужные API совместимы. Откатываться на 15
смысла нет.
shadcn/ui пока не ставился: на этапе 1 интерфейс организатора минимальный,
а гостевые страницы обязаны быть без лишнего JS. Поставлю на шаге 1.2.
Проверено: `npm run build` пройдёт на шаге 0.6, дерево `src/app` на месте.

### 2026-08-18 — 0.2 Окружение и база
Сделано: `.env.example` + `.env` (секрет сгенерирован `openssl rand -hex 32`),
`.env` в `.gitignore`; `docker-compose.yml` (postgres 18 + minio) — для паритета
с продом, локально не используется, Docker не установлен.
Созданы базы `vmeste_dev` и `vmeste_test` в локальном PostgreSQL 18.3.
Проверено: `psql -l` показывает обе базы, расширения `pg_trgm`, `citext`,
`pgcrypto` доступны.

### 2026-08-18 — 0.3 Схема данных и миграции
Сделано: полная схема из PLAN.md §1 (14 моделей), две миграции применены,
клиент сгенерирован в `src/generated/prisma`.
**Прима 7, а не 6** — изменился формат конфигурации: строка подключения
переехала из `schema.prisma` в `prisma.config.ts`, генератор теперь
`prisma-client` с обязательным `output`.
Три вещи, которые пришлось поправить по ходу:
1. `Seat.guest` — связь один-к-одному по составному ключу потребовала
   `@@unique([eventId, guestId])`.
2. **`onDelete: SetNull` на составном ключе — ловушка.** Postgres обнуляет все
   колонки ключа, включая `NOT NULL eventId`, и удаление падает в рантайме.
   Все пять таких связей переведены на `Restrict`; отвязка делается явно
   в репозитории. Комментарий об этом оставлен в схеме.
3. Триграммные индексы сначала были сделаны отдельной SQL-миграцией — Prisma
   сочла их дрейфом и следующей же миграцией удалила. Правильный способ:
   объявить в схеме через `@@index([searchKey(ops: raw("gin_trgm_ops"))], type: Gin)`.
Проверено: `prisma validate` без предупреждений; в БД есть
`guests_searchKey_idx` и `guest_aliases_alias_idx` с `gin_trgm_ops`;
`select similarity('анастасия','настя')` = 0.14 — то есть словарь уменьшительных
действительно обязателен, одними триграммами эта пара не ловится.

**Известная заноза:** `npm audit` показывает 3 high в `deepmerge-ts` — это
транзитивная зависимость `@prisma/config`, работает только в CLI при разборе
конфига, в рантайм не попадает. Фикс — только откат на prisma 6. Оставлено как есть.

### 2026-08-18 — 0.4 Аутентификация организатора
Сделано: `src/server/auth/password.ts` (scrypt из node:crypto — **не argon2 и не
bcrypt**: обе нативные, требуют компиляции и ломают сборку образа на ровном
месте), `src/server/auth/session.ts` (строка в БД + HttpOnly-cookie на 30 дней,
сессию можно погасить), `src/server/context.ts` — `getOrgContext` и
`requireEventContext`. Чужой `eventId` в URL даёт **404, а не 403**: сам факт
существования чужого мероприятия — тоже утечка.
Проверено: косвенно тестом мультиарендности (0.6); страницы входа появятся
на шаге 1.2 вместе с панелью.

### 2026-08-18 — 0.5 Seed
Сделано: `prisma/seed.ts` — две организации, два мероприятия (40 и 8 гостей),
столы с местами, часть гостей рассажена. Имена гостей в двух мероприятиях
**намеренно пересекаются** — иначе утечку между мероприятиями не заметить.
Три раза наткнулся на одно и то же правило Prisma: во вложенном `create`
поля связи с родителем подставляются автоматически, и явный `eventId`
становится ошибкой «Unknown argument». Это же поправлено в репозитории гостей.
Проверено: `npm run db:seed` печатает коды входа; в базе 48 гостей.

### 2026-08-18 — 0.6 Репозитории и заслон мультиарендности
Сделано: `repositories/events.ts` (генерация `shortCode` из алфавита без
похожих символов — ни `0/O`, ни `1/I/l`), `repositories/guests.ts`
(создание с ключом поиска и алиасами, импорт пачкой в одной транзакции,
архивирование вместо удаления), страж в `src/server/db.ts`.
`src/tests/tenancy.spec.ts` — **15 тестов, все зелёные**, проверяют все три слоя:
БД не даёт посадить чужого гостя за свой стол и создать алиас чужому гостю;
репозитории не отдают чужое; страж бросает на запросе без фильтра.
Проверено: `npm test` → 15/15.

### 2026-08-18 — 1.1 Поиск гостя по имени
Сделано: `lib/name-normalize.ts` (нормализация, транслитерация, украинская i,
ё→е), `data/diminutives.ru.yaml` (99 имён с уменьшительными),
`services/diminutives.ts`, `services/guest-search.ts` — три слоя поиска
(точное → префикс → триграммы), порог similarity **0.34**.
Жёсткие ограничения, которые не декоративные: запрос короче двух символов
не возвращает ничего, больше пяти совпадений — просим уточнить. Иначе форма
входа превращается в выгрузку списка гостей для любого, кто знает код.
**26 тестов, все зелёные** (8 на нормализацию + 18 на поиск): уменьшительные,
пропущенная/лишняя/переставленная буква, соседняя клавиша, латиница целиком,
украинская i, обратный порядок имени и фамилии, чужое мероприятие.
Одна правка по ходу: «Dmitry» транслитерировалось в «дмитры» и не совпадало
с «Дмитрий» — добавлено правило конечной `y` → `и` и схлопывание `ии` в конце слова.
Проверено: `npm test` → 26/26.

### 2026-08-18 — 1.4 и 1.5 Вход по QR и «ваш стол»
Сделано: `/e/[shortCode]` (форма) и `/e/[shortCode]/me` (результат: стол,
список совпадений, «не нашли»), рейт-лимит, журнал поисков в `GuestActionLog`.

**Главное решение этапа — эти две страницы написаны БЕЗ React.**
Первая версия была нормальным Next-приложением с клиентской формой. Замер
на продакшен-сборке: **173 КБ и 9 запросов** при бюджете 150 КБ на всё.
Убрал клиентский компонент — стало 173 КБ и 8 запросов: App Router грузит
рантайм React на любой странице, даже полностью серверной.
Поэтому `/e/*` вынесены в обычные route handlers, отдающие HTML строкой,
с инлайновым CSS. Результат: **3.3 КБ и один запрос**. Это в 50 раз меньше
бюджета, и именно этот запас превращает «сеть еле дышит» в «страница открылась».
Форма отправляется обычным GET, поэтому результат имеет собственный адрес —
его можно закешировать в Service Worker и положить в закладки.
Остальные гостевые страницы (приглашение, этап 4) останутся на React: их
открывают дома, а не в дверях зала.
Проверено вживую: `Настя` → Анастасия Петрова, Президиум; `Смиронва` →
Мария Смирнова; `Иванова` → уточняющий список; `Барабанов` → «не нашли вас».

### 2026-08-18 — 1.2 Гости и импорт CSV
Сделано: вход организатора (`/login`), список мероприятий, страница гостей
со счётчиками, добавление вручную, импорт CSV, архивирование.
`services/csv-import.ts` разбирает то, что реально приходит из Excel
(PLAN.md §5.9): определяет кодировку (UTF-8 против Windows-1251 по наличию
символа замены), угадывает разделитель, снимает BOM, восстанавливает телефон
из «9.15E+11», находит колонку с именем по заголовку, а если заголовков нет —
берёт первый столбец и честно об этом предупреждает. Отдельно предупреждает
о полных тёзках в файле: на входе они не различат себя.

### 2026-08-18 — 1.3 Рассадка (упрощённая)
Сделано: `repositories/seating.ts` и страница рассадки — столы создаются
и удаляются, гость сажается на место выбором из списка нерассаженных.
Перетаскивания нет и не должно быть: это этап 3, задача этапа 1 — наполнить
данные, чтобы работал вход в зал.
Две вещи сделаны сразу правильно, чтобы не переделывать на этапе 3:
операции атомарные (`assign`/`clear`/`createTable`), а не «сохранить весь
план», и каждая инкрементит `Event.seatingVersion` — основа для защиты
от двух открытых вкладок (PLAN.md §5.4).
План зала — SVG в условных единицах 1000×700 (PLAN.md §4.7), серверный
компонент без клиентского JS. Один и тот же компонент пойдёт в PDF на этапе 2.

### 2026-08-18 — 1.6 Service Worker
Сделано: `public/sw.js`, network-first с таймаутом 5 секунд, кеш только
на `/e/*`. Если сети нет — отдаётся сохранённая копия страницы «ваш стол»,
если и её нет — страница «нет связи» с советом подойти к координатору.
Регистрируется инлайновым скриптом с обеих гостевых страниц.

### 2026-08-18 — 1.7 Печатные материалы
Сделано: `/app/e/[eventId]/print` — QR-код в SVG (печатается без пикселизации)
и список «имя → стол» по алфавиту в две колонки. Код мероприятия всегда
продублирован крупным текстом: часть телефонов не сканирует QR в темноте зала.
`@media print` разносит табличку и список на отдельные страницы.

### 2026-08-18 — 1.8 Замеры
**Вес (продакшен-сборка, gzip):**
| страница | запросов | вес |
|---|---|---|
| `/e/CODE` — вход по QR | 1 | **3.3 КБ** |
| `/e/CODE/me` — ваш стол | 1 | **2.8 КБ** |
При бюджете 150 КБ. Критерий этапа (30 КБ и 60 КБ) перевыполнен в 10 раз.

**Нагрузка** (150 гостей, продакшен-сборка, локальный PostgreSQL):
| сценарий | p50 | p95 | max | отказы |
|---|---|---|---|---|
| 150 гостей, разные адреса | 12 мс | 27 мс | 65 мс | 0 |
| 150 гостей за одним wi-fi | 19 мс | 71 мс | 86 мс | 0 |
| пик 30 одновременно | 30 мс | 97 мс | 135 мс | 0 |
Критерий этапа — p95 < 300 мс — выполнен с запасом в 3 раза.

**Две вещи, которые нашлись только на нагрузке и которых не было в плане:**

1. **Рейт-лимит по IP при NAT убивает вход.** При лимите 20 запросов в минуту
   на адрес из 150 гостей проходили 17, остальные получали отказ: в зале все
   сидят за одним wi-fi или за одной вышкой, и для сервера это ОДИН адрес.
   Лимит по адресу оказался слепым инструментом для этого сценария.
   Он оставлен грубым потолком против скриптового перебора (300/мин с адреса,
   1200/мин на мероприятие — на два порядка выше живой очереди), а настоящая
   защита от выгрузки списка встроена в сам поиск: минимум 2 символа,
   максимум 5 совпадений, в ответе только имя и стол, каждый поиск в журнале.
   Рассуждение целиком записано в `src/server/rate-limit/client-key.ts`.

2. **Первый запрос после запуска сервера идёт 2,5–4 секунды**, дальше 20 мс.
   Это прогрев Node и пула соединений. В день свадьбы это значит: сервер надо
   разогреть до того, как приедут гости, а не в момент, когда первый человек
   сканирует QR. Добавить в чек-лист дня.

---

### 2026-08-20 — Этап 2: PDF плана рассадки
Сделано: `services/pdf/seating-pdf.tsx` и маршрут
`/api/app/events/[eventId]/seating/pdf`, кнопки выгрузки на страницах
рассадки и печати. Документ на трёх страницах, каждая самодостаточна:
1. **схема зала** (A4 альбомный) — куда идти;
2. **кто за каким столом** — список по столам с номерами мест;
3. **гости по алфавиту** — «имя → его стол», рабочий инструмент координатора
   в дверях.
В подвале каждой страницы отметка времени выгрузки: рассадка меняется
до последнего дня, и на руках не должно оказаться версии недельной давности.
Размер: 28 КБ, генерация 0,5 с, отдаётся потоком.

**Ловушка со шрифтом сработала ровно так, как записано в PLAN.md §4.6 — и даже
хуже, чем ожидалось.** Зарегистрировать Roboto через `Font.register` мало:
`Text` **внутри `Svg`** не наследует шрифт страницы и молча падает
в Helvetica, где кириллицы нет. Заголовки страниц выглядели нормально,
а на схеме зала «Стол 1» превратился в «!B>; 1» — компилятор молчит,
тесты молчат, видно только на выгруженном PDF.
Лечится явным `fontFamily` на каждом SVG-тексте. При этом типы @react-pdf
такого пропса не описывают, хотя рантайм его учитывает, — сделан один
типизированный враппер `PlanText` с объяснением вместо `@ts-expect-error`
в каждой подписи. Проверка: `pdffonts` теперь показывает только
встроенные Roboto, Helvetica исчез совсем.

Ещё две правки по результату просмотра готового PDF:
- подписи мест ставились всегда вниз и наезжали на стол; теперь относятся
  наружу по радиусу от центра стола;
- алфавитный указатель читался поперёк колонок («Александр» слева,
  «Александра» справа); теперь две колонки читаются сверху вниз, левая —
  первая половина алфавита. Координатор, ищущий фамилию, не прыгает
  глазами через страницу.
- символ «→» из подзаголовка убран: его нет в Roboto, в PDF он выпадал
  в мусорный глиф.

Проверено: `pdftoppm` + просмотр всех трёх страниц глазами, `pdftotext`
извлекает кириллицу корректно (документ ищется поиском), `pdffonts`
подтверждает встроенные шрифты; чужое мероприятие и запрос без сессии → 404.

**Заноза дев-сервера:** Next с Turbopack не подхватил правки в модуле,
который импортируется только из route handler, — PDF отдавался старый,
пока сервер не перезапустили. При отладке генерации PDF перезапускать
`npm run dev` после правок.

---

### 2026-08-20 — Этап 3: конструктор рассадки
Сделано: `lib/seating-geometry.ts` (одна геометрия на редактор, просмотр и PDF),
`services/seating-ops.ts` (семь атомарных операций с версией плана),
`api/.../seating/ops`, редактор `components/seating/editor.tsx` с dnd-kit,
`use-seating.ts` (оптимистичный отклик, очередь запросов, отмена),
`tests/seating-ops.spec.ts` — **19 тестов**. Всего в проекте 60 тестов.

Страница рассадки двухслойная: сверху конструктор, снизу те же действия
обычными формами. Формы не рудимент, а запасной путь на случай, если в день
свадьбы на планшете координатора не выполнится JS. Обе ветки идут через
один и тот же `applyOp`.

**Что дал прогон в настоящем браузере.** Логика была покрыта тестами и
проходила, но пять дефектов вылезли только при живом клике — ни один
из них тесты поймать не могли:

1. **Вытесненный гость исчезал из интерфейса.** Если посадить гостя на
   занятое место, сервер честно снимал прежнего, а редактор не показывал
   его в списке нерассаженных: человек пропадал с экрана совсем. В базе
   при этом всё было верно — расхождение видно только глазами.
2. **Попадание при перетаскивании считалось по габаритам карточки с именем.**
   Карточка шириной 200 px перекрывает сразу несколько мест, и гость садился
   не туда, куда его вели. Лечится `collisionDetection={pointerWithin}` —
   попадание по курсору.
3. **Кружок места в 22 px слишком мал.** По нему промахивается мышь,
   а пальцем на планшете не попасть вовсе. Зона нажатия увеличена до 36 px
   (больше нельзя — зоны соседних мест начнут перекрываться), а сам кружок
   оставлен маленьким, иначе план перестаёт читаться.
   Заодно добавлен **второй способ работы: «выбрать и поставить» щелчками** —
   единственный надёжный на тач-экране, и он же работает с клавиатуры.
4. **Расхождение гидрации из-за дробей.** Координата 724/1000 превращалась
   в `72.39999999999999%`, сервер и клиент печатали её по-разному, React
   перерисовывал план целиком и терял состояние редактора. Округление
   до трёх знаков.
5. **Второе расхождение гидрации — от самого dnd-kit**: он нумерует служебные
   идентификаторы сквозным счётчиком, и получалось `DndDescribedBy-0`
   на сервере против `DndDescribedBy-10` на клиенте. Лечится явным
   `id` у `DndContext`.

**Отмена отправлялась трижды.** Первая версия ставила запрос в очередь
и сразу планировала перезагрузку страницы; в замерах версия плана прирастала
на 3 вместо 1. Переписано на одиночный запуск с ожиданием ответа и флагом
`undoing` — измерено: было 5→8, стало 5→6, гость возвращается на прежнее место.

**Панель была тёмной у половины пользователей.** Заготовка `create-next-app`
переключала фон по `prefers-color-scheme`, и у организатора с тёмной темой
системы панель выглядела как светлые карточки на чёрном фоне. Панель теперь
явно светлая: её главный выход — бумага, вторая тема только удваивает
проверку контрастов.

**Ещё одно попадание собственного стража мультиарендности.** Тесты операций
падали, пока в `seat.update` стоял фильтр только по `id`: страж потребовал
`eventId`. Ровно то, ради чего он написан.

Проверено в браузере: выбор гостя щелчком, посадка на свободное место,
пересадка, перенос стола мышью (координаты сохраняются в БД), отмена,
конфликт версий (409 при устаревшей версии), чужое мероприятие → 404,
консоль без ошибок.
**Не проверено: работа пальцем на настоящем планшете** — тач-сенсор с
задержкой 200 мс написан и «выбрать и поставить» специально сделан под тач,
но живого устройства под рукой не было. Это стоит проверить до свадьбы.

---

### 2026-08-20 — Этап 4: приглашение и RSVP
Сделано: схемы блоков `lib/invite-blocks.ts`, репозиторий `repositories/invites.ts`,
сервис ответа `services/rsvp.ts`, разбор форм конструктора `services/invite-forms.ts`,
выгрузка `services/csv-export.ts` + маршрут `/api/app/events/[eventId]/guests/export`,
гостевая группа маршрутов `app/(guest)/i/*` (публичное приглашение, именное,
форма ответа), конструктор `/app/e/[eventId]/invite`, сводка `/app/e/[eventId]/rsvp`.
Тестов: `invite-blocks.spec.ts` (17) и `rsvp.spec.ts` (19), плюс шесть в
`tenancy.spec.ts`. Всего в проекте **102 теста**. Миграции не понадобилось:
модели `InviteBlock`, `MealOption` и поля RSVP у `Guest` заложены на этапе 0.

**Три решения, которые определили остальное.**

1. **Ответ идемпотентен.** Не «лента ответов», а одна строка гостя: жмут
   «отправить» дважды, потом через неделю передумывают. Повторный ответ
   переписывает прежний, отказ обнуляет блюдо — на кухню его заказывать не надо.
2. **+1 — настоящий гость, а не текст в поле.** Иначе спутника не посадить
   за стол, он не найдёт себя на входе по QR и не попадёт в PDF. При ответе
   создаётся `Guest` с `parentGuestId`, `searchKey` и алиасами из словаря
   уменьшительных. Переименование спутника переписывает и ключ, и алиасы;
   снятый спутник уходит в архив, освобождая место за столом.
3. **Форма ответа без единого клиентского обработчика.** Гость открывает её
   с телефона в дороге; она обязана работать до того, как догрузится скрипт.
   Отсюда же отказ прятать выбор блюда при «не сможем»: без JS этого не
   сделать, поэтому блок подписан «если придёте», а сервер игнорирует эти
   поля при отказе.

**Схема JSON-содержимого разбирается дважды, и по-разному.** На записи —
строго: `javascript:` в ссылке обложки и `url(evil)` в палитре до базы
не доходят. На чтении — терпимо и никогда не бросая: если содержимое
разошлось со схемой, уцелевшие поля сохраняются, остальные берутся по
умолчанию, а организатор видит в конструкторе предупреждение. Причина
простая: приглашение открывают по ссылке из смс, и «страница упала из-за
лишнего поля в блоке» — недопустимый исход.

**Что дала проверка живьём (ни одного из этого тесты не видели).**

1. **Публичная страница отдавала страницу ошибки с кодом 200.** `curl -o
   /dev/null -w '%{http_code}'` показывал 200, и это выглядело как успех.
   На деле в логе лежал `RangeError: Invalid time value`: **кеш Next
   сериализует значение в JSON, и `Date` возвращается строкой**, а типы
   при этом остаются `Date` — TypeScript ничего не замечает. Даты теперь
   оживляются на выходе из кеша (`reviveDates`). Урок на будущее: 200 —
   не доказательство, что страница отрисовалась.
2. **`no-store` вместо кеша на публичном приглашении.** Две причины сразу.
   Первая: маршрут с динамическим сегментом без `generateStaticParams`
   Next считает рендером на запрос; пустой список параметров — и появляется
   ISR. Вторая: шаблон заголовков `"/i/:slug/:token*"` со звёздочкой
   совпадает и с нулём сегментов, то есть накрывал саму `/i/:slug`
   и лишал её кеша. Нужен `:token+`.
3. **Отправка формы выглядела как «ничего не произошло».** Гость возвращается
   на верх длинной страницы, а его ответ показан в самом низу, в блоке формы.
   Добавлена полоса «Спасибо, ответ записан: придём» по `?ok=1`.
4. **Ошибки валидации были по-английски** («yandexUrl: Invalid URL»).
   Их читает организатор, а не разработчик: у полей появились человеческие
   названия, у проверок — русские сообщения.
5. **Старый сервер не умирал от `pkill -f "next start"`** (процесс зовётся
   иначе), и три «исправления» подряд проверялись на прежней сборке.
   Убивать по порту: `lsof -ti :3010 | xargs kill -9`.

**Проверено:** ответ гостя целиком в браузере (согласие, спутник, блюдо,
аллергия) — данные в БД совпали, у спутника три алиаса и свой токен;
именная ссылка с чужим слагом → 404; заголовки: публичная страница
`public, max-age=60, stale-while-revalidate=86400`, именная —
`private, no-store` + `noindex`; правка блока сбрасывает кеш и сразу
видна на публичной странице; CSV открывается в Excel (BOM, «;», формулы
экранированы), в нём 40 гостей со ссылками. **Отдельно проверен путь без
JavaScript**: формы конструктора отправлены сырым multipart-POST, как это
делает браузер с отключёнными скриптами, — добавление блока, сохранение
и отказ с русской ошибкой в адресе работают.

**Вес именной страницы: 179 КБ gzip против 150 КБ в плане.** Своей разметки
в этом 5 КБ, остальное — рантайм React и роутера App Router (те самые
~174 КБ, которые на этапе 1 заставили писать вход по QR строкой HTML).
Решение CLAUDE.md «приглашение может использовать React, его открывают
дома» оставлено в силе, но цифра теперь измерена и записана. Если на
реальной свадьбе окажется, что приглашение открывают в дороге, лечится
тем же приёмом, что и `/e/*`: страница целиком в HTML, форма ответа —
обычный POST в route handler; интерактивности на ней нет вовсе.

**Не проверено:** отправка формы с полностью отключённым JavaScript
в настоящем браузере (проверялся эквивалентный ей POST) и вид приглашения
на телефоне — оба стоит пройти на этапе 8.

---

### 2026-08-20 — Этап 5: фотографии и модерация
Сделано: слой хранилища `server/storage/s3.ts`, сервис `services/photos.ts`,
опознание гостя `guest-access/identify.ts`, три маршрута
(`/api/guest/photos/presign`, `/api/guest/photos/complete`,
`/api/media/[eventId]/[photoId]`), загрузчик `components/photos/uploader.tsx`
с резкой превью в браузере, очередь модерации `moderation-queue.tsx`
с горячими клавишами, страница гостя `/i/[slug]/[token]/photos`, страница
организатора `/app/e/[id]/photos`, наполнитель очереди
`npm run db:seed:photos`. Тестов новых 19, всего **121**.
Миграция одна: `Photo.previewOk`.

Хранилище локально — MinIO, поставленный через Homebrew (Docker на машине
нет). Команды подъёма записаны в CLAUDE.md и README.

**Три решения.**

1. **Файл не проходит через наш сервер, а доступ к нему — проходит.**
   Загрузка идёт по подписанной ссылке прямо в бакет: иначе кадр на 8 МБ
   шёл бы через Node дважды, а таких кадров за вечер сотни. Чтение —
   только через `/api/media/...`, потоком, с проверкой прав. Presigned GET
   не используется: ссылка, живущая шесть часов, пережила бы отклонение
   фото, и «убрали с экрана» превратилось бы в «убрали, но по старой
   ссылке видно».
2. **Строка в БД появляется не при выдаче ссылки, а при подтверждении.**
   Между «выдали ссылку» и «файл лежит» нет ничего надёжного: вкладку
   закрыли, связь пропала в лифте. Подтверждение перепроверяет объект
   через `HeadObject`, и размер в БД берётся у хранилища, а не со слов
   клиента. Файл без строки — невидимый мусор; строка без файла была бы
   дырой в галерее.
3. **Превью режет браузер гостя** (canvas → WebP 400 px). Это же лечит
   HEIC с айфонов: организатор на Windows увидит WebP, а не пустую рамку.
   Не получилось — фото всё равно принимаем с `previewOk: false`: потерять
   единственный кадр хуже, чем показать его в модерации без превью.

**Страж мультиарендности снова сработал — на этапе проектирования.**
Первый вариант маршрута был `/api/media/[photoId]`, то есть поиск фото по
одному `id`. Это ровно то, что запрещено правилами доступа к БД. Заводить
исключение (как для `linkToken`) не стали: токен — секрет на 128 бит,
а `photoId` — обычный cuid. Мероприятие переехало в адрес:
`/api/media/[eventId]/[photoId]`. Правило осталось без дырок, а тест
в `tenancy.spec.ts` держит его на месте.

**Что дала проверка живьём.**

1. **Счётчики над очередью врали.** «На модерации 14» оставалось
   серверным, пока очередь внизу показывала 11: решения принимаются без
   перезагрузки. Счётчики переехали внутрь клиентского компонента.
2. **Клавиши, нажатые сразу после появления страницы, не срабатывают** —
   слушатель ставится при гидрации. На ноутбуке это доли секунды, на
   планшете координатора будет заметнее. Лечению в лоб не поддаётся
   (обработчика до гидрации просто нет); стоит помнить при проверке
   на планшете.
3. **Тесты оставляли мусор в бакете.** `resetDb()` чистит базу, а объекты
   оставались: за месяц прогонов это тысячи однопиксельных PNG. Появился
   `deletePrefix`, он же понадобится, когда будут удаляться мероприятия.
4. **Отказы гостю приходили от zod по-английски** («Некорректный
   запрос» на файл в 20 МБ). Схема маршрута теперь проверяет только форму
   запроса, а тип и размер отвергает сервис — «Файл больше 12 МБ».
5. **CORS у MinIO заработал без настройки** — тот самый пункт PLAN.md §5.8,
   на который заложено полдня. Это везение локального MinIO: на проде
   (S3/R2/Spaces) правило CORS с методом PUT придётся прописать руками,
   заготовка положена в README.

**Проверено:** загрузка трёх фотографий с диска в настоящем браузере —
превью нарезаны в canvas, оба файла в бакете, размеры и `previewOk`
в БД совпали; счётчик «осталось N из 5» уменьшается; шестое фото и файл
на 20 МБ отвергаются с человеческим текстом; неодобренное фото — 404 для
анонима, 200 для организатора и 200 для автора по гостевой cookie; после
одобрения — 200 для всех; модерация горячими клавишами (пробел, X, Z)
с живыми счётчиками и без ошибок в консоли; галерея гостя показывает
только одобренное. В тестах — весь путь через настоящий MinIO, включая
подтверждение сверх лимита с уборкой файла.

**Не проверено:** загрузка с настоящего телефона по адресу в локальной
сети (PLAN.md §5.8 просит именно её, а не `localhost`), настоящий HEIC
с айфона (проверялся только путь «превью не получилось») и норматив
«50 фото за 2 минуты» живым человеком — очередь для этого наполняется
командой `npm run db:seed:photos 50`.

---

### 2026-08-21 — Этап 6: экран в зале
Сделано: шина событий `server/events/bus.ts`, сервис `services/screen.ts`,
поток `/api/screen/[token]/stream` (SSE) и снимок `/api/screen/[token]/state`,
страница `/screen/[token]` со своим root layout, компонент
`components/screen/screen-view.tsx`, управление экраном
`/app/e/[id]/screen` с режимами и ссылками для проектора.
Тестов новых 19, всего **140**. Миграция одна: `Event.screenMode`.

**Решения.**

1. **Событие несёт только `{type, id}`, а данные экран перечитывает
   снимком.** Это осознанный размен: снимок — сорок строк, событий за
   вечер сотни. Зато путь обновления один и тот же для первой загрузки,
   обычного события и восстановления после обрыва — один путь, один набор
   ошибок. Плюс потолок `NOTIFY` в 8 КБ, когда шина переедет в PostgreSQL.
2. **Кольцевой буфер на 200 событий** — это и есть ответ на «моргнул
   вайфай». Браузер сам присылает `Last-Event-ID`; если номер ещё в буфере,
   отдаём пропущенное, если нет — `event: resync`, и экран забирает снимок.
3. **Смена кадров на `requestAnimationFrame`, а не `setInterval`.** Вкладка
   в фоне душит таймеры (PLAN.md §5.6); rAF просто не вызывается, и после
   возврата экран встаёт на нужный кадр, а не «догоняет» сотню.

**Что дала проверка живьём.**

1. **Экран, переживший перезапуск сервера, оставался с вчерашней
   картинкой.** Нумерация событий начинается заново, `Last-Event-ID`
   у экрана больше нашего — и `replay` честно отвечал «ничего нового».
   На стене это означает застывший набор до следующего события, а его
   может не быть полчаса. Теперь номер из будущего — повод отдать
   `resync`. Тест закреплён.
2. **Отзыв ссылки не гасил уже открытый экран.** Отозванный токен
   запрещал новые подключения, а живое соединение висело часами: в панели
   было написано «погаснет сразу», и это было неправдой. Теперь на каждом
   пульсе (20 с) проверяется, жив ли токен; если нет — уходит
   `event: revoked`, экран показывает «отключён организатором» и больше
   не стучится. Текст в панели исправлен на честные «в течение 20 секунд».
3. **`export const maxDuration = 60 * 60 * 6` уронил сборку.**
   Конфигурацию сегмента Next разбирает статически и на выражение отвечает
   «Invalid segment configuration export». Нужен литерал: `21600`.
4. **Чёрный кадр на скриншоте оказался не дефектом**, а фазой плавного
   появления: `.screen-fade` длится 700 мс, и снимок поймал начало.
   Стоит помнить при следующих проверках экрана — иначе легко «починить»
   то, что работает.
5. **`npm run build 2>&1 | head -2` рвёт сборку.** `head` закрывает пайп,
   `next build` получает SIGPIPE и умирает на середине, оставляя
   недописанный `.next`, — а следом `npm start` говорит «нет продакшен-
   сборки». Сборку логировать в файл, не в `head`.

**Проверено:** заголовки потока (`text/event-stream`, `no-store,
no-transform`, `x-accel-buffering: no`), живая доставка события с
монотонным `id`; **от одобрения фото до свежего состояния на экране —
1,4 секунды** (требование плана — 2); реконнект с `Last-Event-ID`
отдаёт ровно пропущенное; номер из будущего и незнакомое мероприятие →
`resync`; отзыв гасит живой экран за 18 секунд; страница не
перезагружается (счётчик в `window` пережил все обновления), консоль
чистая; чужой токен и чужое мероприятие — 404 и `false` в тестах.

**Не проверено:** три часа непрерывной работы на утечки памяти и
поведение при уходе ноутбука в сон — это на этап 8, на том самом
ноутбуке, который поедет в зал.
